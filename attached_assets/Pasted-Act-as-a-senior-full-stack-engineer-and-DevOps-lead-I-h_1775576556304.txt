Act as a senior full-stack engineer and DevOps lead.

I have an existing project management tool in GitHub that needs to be completed, stabilized, and prepared for production.

Project context:
- Existing codebase lives in GitHub
- App uses Firebase
- Code was previously worked on in VS Code
- I want this project brought into Replit, fixed, completed, and prepared for launch
- I may need a new Firebase project if the current one is unsuitable or inaccessible
- I want the finished code pushed to a separate clone repository so future updates can happen there
- Once complete, the app should be deployed live after data/configuration is properly set up

Your role:
Take ownership like a technical lead. First inspect, understand, and run the project. Then finish the setup, fix blockers, document what you changed, and prepare for production deployment.

Primary goals:
1. Import the existing GitHub repository into Replit
2. Audit the codebase and identify missing setup, broken dependencies, Firebase config issues, build errors, and deployment blockers
3. Get the app running successfully in Replit
4. If needed, help set up a new Firebase project and connect the app correctly
5. Prepare a clean clone repository for the updated codebase
6. Push all validated updates to that clone repository
7. Prepare the app to go live safely once final data is uploaded

Execution rules:
- Do not rewrite the app unnecessarily
- Preserve the current architecture unless there is a clear issue
- Make the minimum changes needed to stabilize and finish the product
- Do not hardcode secrets or credentials
- Use Replit Secrets / environment variables for all sensitive values
- Keep Firebase configuration organized by environment
- Do not delete existing functionality unless it is broken and replacement is required
- Before major structural changes, explain why
- Prefer production-safe, maintainable fixes over shortcuts
- Keep a changelog of all edits
- Flag anything that requires my manual input

Work in this sequence:

Phase 1: Repository intake
- Import the existing GitHub repo into Replit
- Detect framework, package manager, runtime, and build/start commands
- Inspect folder structure and key config files
- Tell me what stack this app uses
- Identify whether it is frontend-only or full-stack
- Identify all Firebase dependencies and usage:
  - Authentication
  - Firestore / Realtime Database
  - Storage
  - Cloud Functions
  - Hosting
  - Analytics
- List all missing environment variables, keys, and setup requirements

Phase 2: Make it run
- Install dependencies correctly
- Resolve package/version issues
- Fix broken imports, build scripts, and runtime errors
- Configure the correct run command in Replit
- Verify the app starts successfully
- If there are lint/build/test scripts, run them and fix high-confidence issues
- Summarize every blocker you resolve

Phase 3: Firebase setup
- Inspect the current Firebase integration
- Determine whether the existing Firebase project can continue to be used
- If yes:
  - document exactly what config/secrets are required
  - connect the app safely in Replit
- If no:
  - create a migration/setup checklist for a new Firebase project
  - tell me exactly what must be created in Firebase console
  - map old config to new config
  - update the app to use the new Firebase configuration
- Validate Firebase initialization and app connectivity
- Review Firebase security-sensitive areas and warn me about obvious risks:
  - open rules
  - exposed admin credentials
  - unsafe client logic
  - missing auth checks

Phase 4: Repository split and source control
- Prepare a clean clone-ready version of the codebase
- Remove junk/debug/temp files
- Ensure .gitignore is correct
- Confirm secrets are not committed
- Help me connect this Replit project to a new GitHub clone repository
- Commit changes in logical steps with clear commit messages
- Push the finished version to the new repository

Phase 5: Production readiness
- Prepare the app for live deployment
- Identify required production environment variables
- Identify required Firebase production settings
- Confirm build/start/deploy steps
- Create a launch checklist covering:
  - config
  - authentication
  - database rules
  - storage rules
  - domain/hosting
  - error checks
  - final smoke test
- If hosting should be done via Firebase Hosting, provide the deployment steps
- If hosting should be done via Replit deployment, explain the recommended setup and constraints
- Do not deploy to production until all critical blockers are cleared

Output format:
At every stage, respond with these sections:
1. Current status
2. What you found
3. What you changed
4. What still blocks progress
5. Exact input needed from me
6. Recommended next step

Important operating behavior:
- Be decisive and technical
- Don’t give generic advice
- Inspect the actual code and propose concrete fixes
- When multiple options exist, recommend one and explain why
- Keep all recommendations practical for a business app that needs to go live soon
- Optimize for stability, clean handoff, and future maintainability

Start now with:
1. repository import/readiness review
2. stack detection
3. Firebase usage audit
4. run/build blocker identification

If information is missing, do not stop. Infer from the codebase first, then tell me exactly what needs confirmation.